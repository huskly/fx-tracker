# FX Tracker

Static React app for tracking FX positions and FIFO realized lots.

## Run locally

```sh
yarn install
yarn dev
```

## Build

```sh
yarn build
```

The static site is emitted to `dist/`. Deploy that directory to any static host.

## Preview the production build

```sh
yarn preview
```
